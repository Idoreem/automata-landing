# CLAUDE.md - דף הנחיתה של Automata

מסמך הקשר ל-Claude Code. קרא לפני כל שינוי.

## מה הפרויקט

דף סקוויז לקמפיין מטא של Automata (עידו ראם + מאור - מקימי החברה): **פגישת אפיון AI** של 30 דקות, ללא עלות, שממנה בעל העסק יוצא עם "מפת השעות השרופות". ממודל על דף רפרנס של יהב רובין: שני סקשנים (הירו + טופס), CTA אחד, עברית RTL, דארק נייבי.

- **פרודקשן (הכתובת הרשמית):** https://automata-site.vercel.app/landing
- **כתובת ישנה (מפנה אוטומטית):** https://automata-landing.vercel.app
- **פריסה:** כל push ל-`main` נפרס אוטומטית תוך ~20 שניות (Vercel↔GitHub). אין CLI, אין טוקנים.
- הדף חי תחת `basePath: "/landing"` - automata-site מפנה אליו דרך rewrite ב-vercel.json שלו.

## חוקי ברזל - אסור להפר

1. **כל טקסט שגולש רואה חי ב-`lib/copy.ts` בלבד.** שינוי קופי = עריכה שם, לא בקומפוננטות. הקופי עבר סבבי אישור מרובים מול עידו - אל תשנה ניסוחים בלי בקשה מפורשת.
2. **אסור להמציא:** עדויות, מספרים, לקוחות, ROI, "מקומות אחרונים", טיימרים. ההוכחה האמיתית היחידה המותרת: 116 פניות תוך פחות מ-24 שעות משני פוסטים (של עידו). סקרסיטי מזויף נפסל במודע - אל תחזיר אותו.
3. **טרמינולוגיה:** "פגישת אפיון AI" (לא "שיחת מיפוי"), "מקימי Automata" (לא "שותף של"), ואסור לכתוב על מאור "גאון" - הוחלט שזה קרינג'. עידו = אסטרטגיה שמצמיחה עסקים, מאור = הצד הטכני שגורם לזה לעבוד.
4. **CTA אחד בכל הדף** - "קבעו לי פגישת אפיון AI". אין תפריט, אין קישורים החוצה (חוץ ממדיניות פרטיות).
5. **ביטויים אסורים:** "בעולם של היום", "העתיד כבר כאן", "משנה משחק", "מהפכה", טקסט תאגידי/AI-י.
6. אל תוריד את כותרות האבטחה/CSP ב-`next.config.ts` ואל תשנה `basePath` - הכתובת המשותפת תלויה בו.

## מפת הקוד

| קובץ | תפקיד |
|---|---|
| `lib/copy.ts` | כל הקופי + מחרוזות ממשק |
| `lib/site.ts` | `SITE_URL` + `BASE_PATH` - מקור אמת לכתובות |
| `components/Hero.tsx` | הירו: H1 דו-שורתי, סאב, תמונה לחיצה, CTA |
| `components/LeadForm.tsx` | טופס 3 שדות + honeypot + ולידציה בעברית + retry שקט |
| `components/SocialProof.tsx` | עדויות - **מוסתר עד שיש מדיה אמיתית** במערכים `VIDEOS`/`PHOTOS` (יש דגל preview) |
| `components/StickyCta.tsx` | כפתור צף למובייל - רדום כשאין סקשן עדויות, יתעורר לבד |
| `components/MockupSlot.tsx` / `ThanksImageSlot.tsx` | תמונת הנושא (`assets/mockup.png`) |
| `components/MetaPixel.tsx` | פיקסל מטא - כבוי עד שמוגדר `NEXT_PUBLIC_META_PIXEL_ID`; אירוע Lead עם דדופ |
| `app/api/lead/route.ts` | קליטת ליד: ולידציה, הגבלת קצב (20/דקה/IP), honeypot→דגל, webhook עם timeout |
| `app/thanks/page.tsx` | דף תודה: קונפטי, בלוק הכנה, תמונה |
| `app/privacy/page.tsx` | מדיניות פרטיות |

## מוקשים ידועים (נלמדו בדם)

- **bidi:** רצף `AI - 30` מתערבב בעברית. תמיד "AI של 30". אחרי כל שינוי קופי עם מספרים/לטינית - לבדוק ויזואלית.
- **basePath:** ‏`fetch()` ידני ולינקים ב-`<a>` רגיל לא מקבלים prefix אוטומטית - להשתמש ב-`BASE_PATH` מ-`lib/site.ts`. ‏`Link`/`router` כן מקבלים.
- **PowerShell הורס עברית** ב-echo/Set-Content. עריכת קבצים עם עברית - רק דרך כלי הקבצים של Claude.
- **פריסות נכשלות בשקט:** אחרי push תמיד לוודא מול האתר החי עם מחרוזת שקיימת רק בגרסה החדשה (grep על ישן = false positive).
- אין ESLint בפרויקט בכוונה. אל תוסיף בלי צורך.

## איך בודקים (חובה לפני שמסיימים)

```bash
npm run build          # חייב לעבור נקי
npm run qa:flow        # E2E: מילוי טופס → דף תודה (שולח ליד בדיקה אמיתי!)
npm run qa:widths      # אפס גלישה אופקית ב-320/360/430/768px
npm run qa:assets      # אפס שגיאות טעינה + תמונת הירו
```

הסקריפטים רצים נגד `http://localhost:3000/landing` כברירת מחדל (`npm run dev` קודם), או נגד כל URL: `npm run qa:flow -- https://automata-site.vercel.app/landing`. דורשים Chrome/Edge מקומי (זיהוי אוטומטי; אפשר `BROWSER_PATH=`).

רף איכות שחובה לשמור: Lighthouse ביצועים ≥95, נגישות 100. מובייל תמיד נבדק ראשון - 90% מהטראפיק.

## משתני סביבה (Vercel של עידו → automata-landing)

| משתנה | סטטוס | הערה |
|---|---|---|
| `LEAD_WEBHOOK_URL` | ⚠️ חסר - לידים רק נרשמים ללוג! | Webhook של Make/CRM. בלעדיו אסור להזרים טראפיק |
| `NEXT_PUBLIC_META_PIXEL_ID` | ⏳ חסר | מדליק פיקסל + אירוע Lead |

שינוי env = דרך עידו (החשבון שלו ב-Vercel), ואז Redeploy.

## פתוח כרגע

1. חיבור `LEAD_WEBHOOK_URL` (הטוקן/Webhook אצל מאור ב-CRM).
2. Pixel ID ממטא.
3. סרטוני עדויות + תמונות → ממלאים את המערכים ב-`SocialProof.tsx` והסקשן קם לבד (כולל תמיכה בכתוביות `.vtt`).
4. דומיין אמיתי ל-Automata - כשנקנה: עדכון `lib/site.ts` + חיבור ב-Vercel, הקוד מוכן.
